# Database (for local Docker: postgresql://didact:didact@localhost:5432/didact)
DATABASE_URL=postgresql://didact:didact@localhost:5432/didact

# Authentication
JWT_SECRET=your-secret-key-change-in-production

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# App configuration
NUXT_PUBLIC_APP_URL=http://localhost:3000
NUXT_PUBLIC_APP_NAME=Didact

# Email - Mailhog (local development)
# docker compose up will start Mailhog on port 1025 (SMTP) and 8025 (Web UI)
# No configuration needed for Mailhog - defaults work automatically

# Email - Production SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@example.com
# SMTP_PASSWORD=your-password
# SMTP_TLS=true
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_FROM_NAME=Didact

# Storage - MinIO (local development)
# Uncomment these lines to use MinIO for local S3-compatible storage
# docker compose up will start MinIO on port 9000 (API) and 9001 (Console)
# STORAGE_ENDPOINT=http://localhost:9000
# STORAGE_ACCESS_KEY=minioadmin
# STORAGE_SECRET_KEY=minioadmin
# STORAGE_BUCKET=didact-uploads
# STORAGE_PUBLIC_URL=http://localhost:9000/didact-uploads

# Storage - Cloudflare R2 (production)
# STORAGE_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# STORAGE_REGION=auto
# STORAGE_ACCESS_KEY=your-access-key
# STORAGE_SECRET_KEY=your-secret-key
# STORAGE_BUCKET=didact-uploads
# STORAGE_PUBLIC_URL=https://uploads.yourdomain.com
